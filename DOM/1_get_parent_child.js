// get parent node
let form = document.querySelector('.note');
console.log(note.parentNode);

// get the first child
let form2 = document.querySelector('.note');
console.log(note.firstChild);

// get the last child
let form4 = document.querySelector('.note');
console.log(note.lastChild);

// get the last child
let form5 = document.querySelector('.note');
console.log(note.allChilds);
